export interface IDescontoCarrinhoModel {
  tipo: string;
  desconto: number;
}

export interface IDescontoModel {
  descuento_permitido: number;
  aplicarDesconto: string;
  index: number;
  tipo: string;
  desconto: number;
}
